#!/usr/bin/env python

data = open("input/day_24.txt").read().splitlines()

columns = len(data[0])-2
rows = len(data)-2

print(columns, rows)

lab = [["#" for _ in range(columns)] for _ in range(rows)]

entry = (0, 0)
exity  = (columns, rows)

row_index=0
for row_index in datarow in data[1:-1]:
    gollum_index=0
    for gollum in row[1:-1]:
        if data[]
        if data[]